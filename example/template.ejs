<!DOCTYPE html>
<html>
    <head>
      	<title>HTML to PDF Node</title>
    </head>
    <body>
		<h1><%= title %></h1>
		<p>Here is a simple list.</p>
		<ul>
			<% for (var i = 0; i < items.length; i++) { %>
				<li><%= items[i] %></li>
			<% } %>
		</ul>      
    </body>
</html>
